# Ferramentas

ECRVSP Documentos: Placas de Primeiro Emplacamento expõe 1 ferramenta (todas somente leitura).

### 1. `ecrvsp_docs_emplacamento_lista_consultar`
**Input**: `a3`, `a3_pin`, `login_cpf`, `login_senha`, `chassi`, `categoria`, `tipo_restricao`, `cpf` (opcional), `cnpj` (opcional), `nfe` (opcional), `nfe_data_emissao`, `veiculo_pessoa_deficiencia` (opcional), `veiculo_taxi`, `veiculo_isencao_fiscal` (opcional), `veiculo_venda_dispensada_renave` (opcional), `veiculo_pessoa_deficiencia_venda_direta` (opcional), `veiculo_empresa_salvados` (opcional)

ECRVSP Documentos: Placas de Primeiro Emplacamento, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar ECRVSP Documentos: Placas de Primeiro Emplacamento
```
